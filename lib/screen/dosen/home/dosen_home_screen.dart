import 'package:flutter/material.dart';

class DosenHomeScreen extends StatelessWidget {
  const DosenHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Dosen Home Screen"),
      ),
    );
  }
}
