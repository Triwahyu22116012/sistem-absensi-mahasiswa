import 'package:flutter/material.dart';

class JadwalPerkuliahanScreen extends StatelessWidget {
  const JadwalPerkuliahanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Jadwal Perkuliahan"),
      ),
    );
  }
}
